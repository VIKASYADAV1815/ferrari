'use client';

// Lenis is now handled in HeroVideo component for scroll-synced video
// This component is kept for compatibility but does not initialize Lenis
export default function SmoothScroll() {
  return null;
}
